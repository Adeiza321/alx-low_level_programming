#include "main.h"
/**
 * _islower - This function checks for lowercase
 * @c: The value checked
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

