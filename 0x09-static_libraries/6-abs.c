#include "main.h"

/**
 * _abs - This function computes value of an integer
 * @n: the value to check
 * Return: the value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
